---
interface Props {
  name: string;
  label: string;
  type?: 'text' | 'email' | 'tel' | 'url' | 'number';
  required?: boolean;
  autocomplete?: string;
  values?: Record<string, string>;
  errors?: Record<string, string>;
  placeholder?: string;
}

const {
  name,
  label,
  type = 'text',
  required = false,
  autocomplete,
  values = {},
  errors = {},
  placeholder,
} = Astro.props as Props;

const value = values[name] ?? '';
const error = errors[name];
const id = `field-${name}`;
---

<label for={id} class="grid gap-1.5">
  <span class="t-ui">
    {label}{required && <span aria-hidden="true" class="text-[var(--color-brand)]"> *</span>}
  </span>
  <input
    id={id}
    name={name}
    type={type}
    required={required}
    autocomplete={autocomplete}
    placeholder={placeholder}
    value={value}
    aria-invalid={error ? 'true' : undefined}
    aria-describedby={error ? `${id}-error` : undefined}
    class="t-body-sm rounded-md border border-[var(--color-border)] bg-[var(--color-canvas)] px-3 py-2 text-[var(--color-ink)]"
  />
  {error && <span id={`${id}-error`} class="t-caption text-[var(--color-brand)]">{error}</span>}
</label>
