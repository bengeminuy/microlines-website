---
interface Props {
  class?: string;
}
const { class: className = '' } = Astro.props as Props;
---

<div class:list={['grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3', className]}>
  <slot />
</div>
